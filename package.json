{
  "name": "dan-express-mongo-rwieruch",
  "engines": {
    "node": "8.11.4",
    "npm": "5.6.0"
  },
  "scripts": {
    "start": "npm run server --prefix server",
    "heroku-prebuild":"npm install --prefix server --only=dev",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix client --only=dev && npm run build:dev --prefix client"
  }
}
