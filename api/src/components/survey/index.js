import surveyModel from './surveyModel';
import surveyAPI from './surveyAPI';
import surveySchema from './surveySchema-graphql-compose';
import surveyTypeDefs from './surveyTypeDefs';
import surveyResolver from './surveyResolver';

export default {
  surveyAPI,
  surveyModel,
  surveySchema,
  surveyTypeDefs,
  surveyResolver
};
